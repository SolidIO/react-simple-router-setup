# react-simple-router-setup
A simple project setup that uses react router

Setup:

Clone it.
Run "npm install".
Run "bower install"
Run "gulp serve" for livereload
To build project with minified code

Run "gulp clean_dist" (No demand, but nice)
Run "gulp"
All your files are in dist/